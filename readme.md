# Laundry Services Button Interactive

## Project Description

This project is a simple Laundry Services webpage created using **HTML and CSS**.

The main purpose of this task is to create an interactive **Call-to-Action (CTA) button** using CSS transforms.

When the user moves the mouse over the CTA button:

- The button increases in size.
- The button tilts slightly.
- The transition happens smoothly.
